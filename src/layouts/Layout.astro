---
import '../styles/global.css';
import type { Lang } from '../i18n/ui';
import { htmlLang, useTranslations } from '../i18n/ui';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

// The document <title> (browser tab, search results, social cards) stays in
// plain French for the Lorrain locale, where the gag would just read as garbled.
const titleT = lang === 'lo' ? useTranslations('fr') : t;
const title = `${titleT('site.title')} - ${titleT('site.tagline')}`;
const description = t('site.intro');

// Absolute URLs need the configured `site` (SITE_URL); fall back gracefully if
// it is missing so a dev/preview build never crashes.
const base = Astro.site;
const canonical = base ? new URL(Astro.url.pathname, base).href : Astro.url.href;
const ogImage = base ? new URL('/og.png', base).href : '/og.png';
const frUrl = base ? new URL('/', base).href : '/';
const enUrl = base ? new URL('/en/', base).href : '/en/';
const loUrl = base ? new URL('/lorrain/', base).href : '/lorrain/';
// Lorrain is French-based, so it shares the fr_FR Open Graph locale.
const ogLocale = lang === 'en' ? 'en_US' : 'fr_FR';
const altLocales = ['fr_FR', 'en_US'].filter((l) => l !== ogLocale);
---

<!doctype html>
<html lang={htmlLang(lang)}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <meta name="generator" content={Astro.generator} />

    <link rel="canonical" href={canonical} />
    <link rel="alternate" hreflang="fr" href={frUrl} />
    <link rel="alternate" hreflang="en" href={enUrl} />
    <link rel="alternate" hreflang="fr-x-lorrain" href={loUrl} />
    <link rel="alternate" hreflang="x-default" href={frUrl} />

    <meta property="og:type" content="website" />
    <meta property="og:site_name" content={t('site.title')} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={canonical} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:locale" content={ogLocale} />
    {altLocales.map((l) => <meta property="og:locale:alternate" content={l} />)}

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <a class="skip-link mono" href="#content">{t('a11y.skip')}</a>
    <slot />
  </body>
</html>
